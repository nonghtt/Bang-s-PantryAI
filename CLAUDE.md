# PantryAI — 작업 지침

## 이 프로젝트의 성격

**3년 차 풀스택 개발자의 취업용 포트폴리오**다. 동작하는 제품을 만드는 것이 목표지만, 최종 평가자는 사용자가 아니라 **채용 담당자와 면접관**이다.

그래서 이 프로젝트에서 **"무엇을 만들었는가"보다 "왜 그렇게 만들었는가"가 더 중요하다.** 코드는 AI로 빠르게 쓸 수 있는 시대이므로, 변별력은 판단의 흔적에서 나온다.

---

## 의사결정 기록 규칙 (가장 중요)

**기술 선택이나 설계 결정을 내릴 때마다 반드시 두 가지를 함께 한다.**

### 1. `docs/career-source.md`에 기록한다

이 파일은 **씨드**다. 사람이 읽지 않는다 — 세션이 읽고, 세션이 여기서 **보고서를 생성**한다. `korea-doc`이 읽는 것은 그 보고서다.

따라서 **씨드는 `career.md`의 산문 형식을 따르지 않는다.** 검색과 선별에 최적화된 고정 필드 형식을 쓴다.

본문에 남길 것은 다섯 가지다.

| 라벨 | 내용 |
| --- | --- |
| `**문제**` | 왜 이 결정이 필요했는가 — 무엇이 문제였는가 |
| `**선택지**` | 무엇과 무엇을 놓고 고민했는가 (**버린 쪽도 반드시 적는다**) |
| `**선택**` · `**근거**` | 무엇을 택했고 왜인가 — 트레이드오프를 구체적으로 |
| `**결과**` | 그래서 무엇이 달라졌는가 — 가능하면 수치로 |
| `**한계**` | 반례·미검증·측정하지 않은 것. **있으면 반드시 적는다** |

형식은 다음과 같다. `career.md`의 산문 형식(**"문제: A vs B → 선택과 근거"**)은 씨드가 아니라 **보고서 생성 시** 적용한다.

```markdown
## D-034 제목
- date: YYYY-MM-DD
- status: live            # live | refined(+refined_by) | superseded(+superseded_by)
- scope: 정규화           # 재고모델·데이터소스·카테고리·지표·파이프라인·정규화·아키텍처·측정·제품범위
- basis: 실측             # 실측 | 도메인지식 | 제품목적 | 관례
- verified: yes           # yes | no | partial  ← 코퍼스로 검증됐는가
- strength: high          # high | mid | low    ← 보고서 선별용
- story: 검증을_먼저      # 측정이_판단을_뒤집음·트레이드오프를_수치로·배제_판단·전제_오류_수정
                          # ·검증을_먼저·정직성_경계·자동화_함정·실무경험_적용
- implements: D-010       # (선택) 상위 원칙의 사례일 때

**문제** …
**선택지** A / B
**선택** …
**근거** …
**결과** …
**한계** …          ← 반례·미검증·측정 안 한 것. 있으면 반드시 적는다
```

**기록 후 반드시 `python scripts/career_index.py`를 실행한다.** 인덱스를 재생성하고 ID 중복·끊어진 참조·관계 비대칭·수치 불일치를 검사한다. 오류가 있으면 인덱스를 쓰지 않고 멈춘다. **인덱스는 직접 편집하지 않는다.**

**씨드는 줄이지 않는다.** 버린 선택지와 수치가 이 자료의 가치다. 미니멀화는 보고서 쪽에서 선별로 한다.

**보고서로 쓴다.** 학습용 설명이나 코칭 톤을 쓰지 않는다.

> ⚠️ **각색이나 화법은 저장하지 않는다.** `career.md` 작성 규칙이 *"특정 기관을 향한 각색은 저장하지 않는다 — 각색은 작성 시점에 생성한다"*이기 때문이다. 여기에는 사실과 수치만 남기고, "이렇게 말하면 된다"는 쓰지 않는다.

### 2. 사용자에게 대화로 알린다

**파일에 쓰고 끝내지 않는다.** 그 턴의 응답에서 "경력 자료로 남겼습니다"라고 명시하고, 무엇을 어떤 맥락으로 기록했는지 한두 줄로 요약해 준다.

> 이유: 나중에 몰아서 회고하면 결정 당시의 근거와 고민의 밀도가 날아간다. 기록은 결정이 살아 있는 시점에 해야 한다.

### 무엇을 기록 대상으로 볼 것인가

- ✅ **트레이드오프가 있었던 것** — 다른 선택지가 실재했고, 어느 쪽이든 갈 수 있었던 결정
- ✅ **무언가를 포기하거나 배제한 것** — 배제 근거는 그 자체로 강한 재료다. 실제로 만개의레시피(CC BY-NC-ND) 배제 판단이 이 프로젝트의 가장 강한 답변 중 하나가 됐다
- ✅ **데이터나 측정이 판단을 바꾼 것** — 수치와 함께 남긴다
- ❌ 관례를 그대로 따른 것, 선택지가 사실상 하나였던 것

---

## 정직성 원칙

**경계를 넘지 않는다.** 하지 않은 것을 한 것처럼 쓰지 않는다.

`career.md`에서 gs-retail 프로젝트를 *"이관 설계 경험은 아님 — 서술 시 이 경계를 넘지 말 것"* 이라고 스스로 못박아둔 것과 같은 기준을 적용한다.

- **"만들었다"와 "쓰인다"를 구분한다.** 실사용자가 없다는 사실을 숨기지 않는다
- 측정하지 않은 것을 측정했다고 하지 않는다
- 생성한 데이터와 검증된 데이터를 라벨로 구분한다

---

## 이 프로젝트의 차별점 — 판단 기준

기능 우선순위나 구현 방향을 정할 때 이 축을 기준으로 삼는다.

> **"동작한다"가 아니라 "얼마나 정확한지 측정된다".**

LLM 포트폴리오의 대다수는 "붙였다"에서 멈춘다. 이 프로젝트는 **개선 전후를 수치로 비교**하는 것을 목표로 한다. 관측·정합성·정량 검증은 이 개발자의 실무 서명 강점(1,400ms→10ms, 잔차 0 대조)과 직결되므로, 여기에 투자하는 선택이 대체로 옳다.

기준선은 이미 확보되어 있다 — 재고 커버리지 4.9%, 카테고리 분류 정답 세트 84건.

---

## 제품 성공의 정의

기능 우선순위 다툼이 생기면 이 문장으로 자른다.

> **사용자가 3주 뒤 "비슷한 식재료로 다양한 음식을 해먹었구나"라고 느낀다.**

재고 정확도는 성공이 아니라 수단이다. 자세한 내용은 브리프의 `Success Criteria` 참조.

---

## 문서 위치

| 문서 | 내용 |
| --- | --- |
| `_bmad-output/planning-artifacts/briefs/brief-PantryAI-2026-08-13/brief.md` | 제품 브리프 — 문제·해법·범위·성공 기준 |
| 같은 폴더 `addendum.md` | API 규격 · 데이터 상세 · 배제 근거 · 아키텍처 갈림길 |
| `docs/career-source.md` | **경력 씨드** — 사람이 읽지 않는다. 세션이 읽고 보고서를 생성한다 (gitignored, 로컬 전용) |
| `scripts/career_index.py` | 씨드 인덱스 생성·검사. 씨드를 고친 뒤 실행한다 |
| `data/processed/report.md` | 정제 결과 검증 보고서 — 스크립트가 생성한다. 직접 편집하지 않는다 |

---

## 데이터 파이프라인

애플리케이션 아키텍처와 무관하게 먼저 확정된 부분이다.

```powershell
# PowerShell (기본 셸)
$env:PYTHONIOENCODING = 'utf-8'

python scripts/build_processed.py          # 원천 -> 정제 산출물 전량 재생성 (멱등)
python -m unittest discover -s tests       # 파서 규칙 테스트
```

```bash
# bash 계열
PYTHONIOENCODING=utf-8 python scripts/build_processed.py
PYTHONIOENCODING=utf-8 python -m unittest discover -s tests
```

- **Python 3.14를 직접 쓴다.** 이 환경에는 `uv`가 없고, 테스트는 외부 의존성 없이 표준 라이브러리 `unittest`로 짠다.
- 콘솔 인코딩 문제를 피하려면 `PYTHONIOENCODING=utf-8`을 설정한다.
- `data/raw/`는 **읽기 전용**이다. 절대 수정하지 않는다.
- `data/processed/`는 전량 생성물이다. 손으로 고치지 말고 스크립트를 고쳐 다시 만든다. (`report.md` 포함)
- 파서 규칙(`scripts/stage1/`)을 바꿀 때는 **테스트를 먼저 쓴다.** 한 형태를 고치면 다른 형태가 조용히 깨진다.
- `build_processed.py`는 입력·출력 행 수 대조를 `assert`로 자체 검증한다. **잔차가 0이 아니면 산출물을 쓰지 않고 멈춘다.** 이 검증을 우회하지 않는다.

### 판단은 다음 단계로 미룬다

1단계 정제는 두 소스를 같은 모양으로 맞추는 것까지만 한다. 재료명 표준화·주재료 판정·카테고리 분류·중복 판정·데이터 생성은 **하지 않는다.** 규칙이 확신하지 못한 것은 버리지 말고 `unparsed_ingredients.json`·`orphans/`로 분리해 남긴다.

---

## 아직 정해지지 않은 것

애플리케이션 기술 스택·폴더 구조·코딩 컨벤션은 **아키텍처 확정 후 이 문서에 추가한다.**

`AGENTS.md`는 만들지 않는다 — 이 문서 하나로 통일한다.
